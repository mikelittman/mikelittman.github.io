{
  "name": "mikelittman",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "_push": "git push origin `git subtree split --prefix out origin gh-pages`:gh-pages --force",
    "deploy": "rm -rf node_modules/.cache && next build && next export && touch out/.nojekyll && cp CNAME out/ && git add -f out/ && git commit -m \"Deploy Next.js to gh-pages\" && npm run _push"
  },
  "dependencies": {
    "next": "14.0.1",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@types/node": "18.18.8",
    "@types/react": "18.2.36",
    "@types/react-dom": "18.2.14",
    "eslint": "8.13.0",
    "eslint-config-next": "12.1.4",
    "prettier": "2.6.2",
    "typescript": "4.6.3"
  },
  "prettier": {
    "printWidth": 60
  }
}
